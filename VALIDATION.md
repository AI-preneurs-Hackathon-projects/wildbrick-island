Interface stage: desktop supported-browser HUD and first-play tour inspected through the development-only controls harness. Eleven DOM/scene checks cover first-play, Skip, once-per-device storage, Help reopen, touch-specific tour text, no Imagine/Enter shortcut and fallback focus. This harness has no WebGL or live arena backend.

Generation baseline: three fresh production requests using the unchanged version-11 gpt-5.4 configuration are recorded under `validation/live/baseline-v11/`. Dragon failed validation after 40.45 s; car succeeded with 44 parts in 28.68 s request time (15.48 s server generation); octopus succeeded with 49 parts in 53.54 s (21.27 s server generation). CPU validation/model construction were under 15 ms combined on successful requests. Speech, actual network decomposition and GPU upload are not measured. The gap between upstream and request time means model speed alone cannot explain all waiting.

Generation instrumentation and admission stage: two new checks exercise actual migration SQL for concurrent global slots, per-requester/minute limits and compact v4 versus legacy bounds. The historical captured replay test now checks movement during the shorter 1.2 s assembly. Model/profile selection is pending live comparison; no fabricated generation is substituted.

Combat stage: timed punches do no damage before their windup; one strike causes one confirmed hit, retries do not repeat damage, and out-of-range swings miss. Projectile contact produces one hit event at its actual contact, without an extra duplicate impact. Scene assertions check visible arm extension, sword rotation, empty hands, target flash/flinch and unchanged actor position. Existing armor, creation destruction, cover, pickups, KOs and respawn checks pass. Presentation and audio remain unverified in rendered WebGL and across real signed-in devices.

Movement stage: swept-wall/corner/tunneling tests pass for foot, car, plane and captured dragon geometry. A two-metre gap admits a walker and blocks a large mount; authoritative/predicted house blocking agrees. Roof overflight, lowering onto roofs, safe failed assembly and airborne dismount retain pose. Seeded 3,600-frame movement has no recovery jumps. All 13 simulated latency/retry/respawn movement checks pass unchanged. DOM/scene checks verify local landmark fading without changing camera position. Supersedes historical universal pass-through assertions; no WebGL render claim.

# Brickwild validation — 2026-09-12

## Current status

The existing owner-private Site and source repository were recovered and preserved. The initial checkout was clean at `75ce42dba2e6456eb8503941a31fac985493c166`; an authenticated fetch found no newer work before changes. No audience change was made.

The OpenAI connection is active. The owner-provided key is a masked server-side Sites secret. Environment revision 3 selects `gpt-5.4`; the final source uses the same default. A real model-access request returned HTTP 200. Actual production generation outputs are saved under `validation/live/`, with prompts and provenance. They are not public game assets, presets, or fallback responses.

## Automated checks

The pre-arena `npm run check` suite ran 42 checks: 16 original checks, nine synthetic generation checks, 14 adventure/geometry checks, and three replays of captured live OpenAI outputs. It does not call the paid API. That historical run used committed Three.js 0.180.0 and Rapier 0.19.0. The latest revision removes Rapier from runtime and deployment; see the latest-fix section below.

The original suite verifies movement gating and direction, running during construction, collision and reversing, takeoff/altitude/landing/jump, camera obstruction, bow targeting and line of sight, sword proximity, recovery, and all 15 original objectives with exactly 226 reward bricks and one completion event. All original model constructors and static asset references are checked.

The generation and adventure suites cover bounded geometry and invalid output rejection, strict API request data, full free-form prompt forwarding, missing-key/refusal/error responses, generated flight/pulse and scenery collisions, saved progress and deduplication, corrupt/unavailable storage, cancellation and out-of-order responses, useful timeout feedback, streamed request body caps, two-request concurrency, upstream cancellation, raw multi-segment speech, optional voice fallback, model-access verification without secret disclosure, grip normalization, root-coordinate articulation, supported rider seats, and wheel axle orientation.

Captured live-output replays instantiate each actual blueprint, check finite animated matrices and draw-call limits, move during assembly, automatically enter the appropriate movement mode, fly or drive, score a compatible objective, fire the dragon pulse, and save/rebuild the blueprint without a new API call. These are Node integration checks, not rendered browser playtests.

## Real API and geometry review

An initial GPT-5 Mini dragon request reached the 85-second upstream timeout. GPT-4.1 Mini returned quickly but failed silhouette review: detached dragon pieces, missing octopus arms, incorrect wheels and an unclear teapot. Those findings led to root-coordinate blueprint version 2, a 32-part minimum, clearer anatomy instructions and selection of GPT-5.4. Version-1 saved designs remain loadable.

GPT-5.4 generated a 54-part emerald dragon with seven joints and a 49-part purple mechanical octopus with 12 joints. The dragon model call took 38.386 seconds (51.661 seconds total HTTP); the octopus took 22.983 seconds (32.674 seconds total). The dragon has a connected torso/neck/head, golden mirrored wings, legs, tapering tail and a supported saddle. The octopus has eight articulated tentacles, copper details and four wheels. Exact-triangle CPU diagnostics exposed an embedded octopus seat and wheel-axis mismatch; the generic renderer now places seats on fixed geometry and aligns spinning cylinders with their declared axle. Rest and moving poses were reviewed after those fixes.

The successful 37-part turquoise flying teapot model call took 34.370 seconds (47.104 seconds total). During this real request, `scripts/check-live-request.mjs` kept the actual creation service and Rapier simulation running: 334.06 units traveled while waiting, 19.72 during assembly, automatic mounting, then flight to 12.21 units. This is a live HTTP plus Node integration test, not a browser playtest.

The final teapot CPU views show a rounded body, connected curved spout, open rear handle, golden lid and mirrored wings. The rendered seat is supported. All three final designs passed this limited geometry review; the images are saved beside the captured responses.

One stronger-model teapot request returned the explicit invalid-design error. No partial or substituted creation was installed. Generation remains probabilistic: shape quality varies and an invalid design can require a retry. Bounds validation establishes renderable data, not universal semantic correctness.

`node scripts/render-blueprint.mjs` imports the actual renderer, extracts its instantiated triangles and produces front, side and three-quarter CPU images plus seat surface intersections. NumPy/Pillow are development-only dependencies. These diagnostics show real generated geometry, but use simplified lighting and are not browser screenshots.

## Browser and device limits

A supported Vite development preview was added and started successfully. The supported cloud browser reached the game but could not create a WebGL context: both GL_VENDOR and GL_RENDERER reported Disabled. The graceful graphics-unavailable screen was observed. Browser settings and flags were not bypassed.

Actual gameplay rendering, camera/HUD appearance in WebGL, real multitouch, microphone recognition, speech permission prompts and physical iPad performance remain unverified. Automated recognizer doubles verify denied/unsupported speech fallbacks and transcript lifecycle only. The browser/device limit must not be described as a passing playtest.

Private Sites sign-in is required to access the owner-private game. Speak is optional and may require microphone/speech permission; Imagine remains available without it. The game itself requests no camera, location, contacts or notifications. The user's exact device permission prompt was not supplied, so it cannot be identified more precisely.

## Security and performance

Secrets are runtime-only. Client code has no key-entry form or OpenAI secret; the public package contains vendored engines, fonts and preserved third-party licenses. Requests accept only a description, never executable model code. The guard allows at most 360 characters, 2,048 body bytes (including chunked bodies), 12,000 output tokens, 85 seconds upstream, eight requests/minute/IP/isolate and two concurrent upstream requests/isolate. These in-memory guards are not a durable global quota or spend cap. Owner-private Sites access remains the primary access boundary. Cancellation cannot guarantee an already-running API request is unbilled.

Generated objects use at most five primitive instance batches, 128 parts, 16 joints and 256 studs. Replaced instances dispose their owned resources. Up to six placed objects remain active. Scene geometry remains the original procedural island; the earlier static count was approximately 310,000 world triangles and 267 world draw objects before player/equipment. Resolution is capped and can fall to 1× on slow frames. Physical device frame rate is not measured.

The self-contained Worker build emits `dist/server/index.js` with 29 public assets. Development packages, captured validation outputs and preview credentials are excluded from deployment. Compiled-Worker checks returned all 29 assets byte-for-byte identical to source, confirmed that validation files and preview credentials return 404, and found no OpenAI secret reference or key literal in public assets. The supported preview was stopped and its temporary credential removed after QA.


## Physics and multiplayer arena update

Work continued from saved commit `789cff64c289c6fca91d605b79f252556548d21d`; a fresh source fetch found no newer work. The sole-owner private audience and server-side OpenAI secret were retained. No credential is part of source or deployed public assets.

At the arena release, the suite contained **69 checks**: the prior 42, one new captured version-3 live-output replay, plus 22 arena simulation/actual SQLite-backed API checks and four DOM/Three.js scene checks. The new checks cover eight seats, three distinct authenticated test identities sharing a room, concurrent compare-and-swap updates, atomic session caps, full-room cleanup, request bounds, cross-viewer token rejection, server-owned stats/positions/scores, stale inputs/commands, terminal seat expiry, duplicate creation uploads and membership removal. SQLite executes the committed generated migrations and real SQL through an asynchronous D1-compatible adapter. This does not claim production latency or multi-device network performance.

Physics/combat checks cover swept oriented hulls, boosted wall collision, rotation beside a building, all five ring openings admitting an aligned plane, heavy cover destruction, crash ejection, mount shield absorption, one KO per death, 12-second respawn, spawn protection, logical weapon/mobility tradeoffs, full-range bow hits, projectile cover occlusion, melee facing/height/cover, automatic overheat, one-collector pickups, ten-second buff timers, static creation placement, and moving assembly into a flying flame-equipped mount. A conservative server geometry metric matches the actual Three.js renderer on the three captured live designs; degenerate geometry and outlying emitters are rejected or bounded.

The DOM checks exercise arena entry, room/health/leaderboard binding, escaped names, independent fire and jump inputs, pointer cancellation, full vertical aim range, finite remote scene matrices, construction and geometry disposal. They use jsdom and a minimal canvas stand-in. They do **not** exercise WebGL pixels, browser microphone recognition, physical multitouch or iPad frame rate. The earlier cloud browser's WebGL-disabled limitation remains.

New runtime uses small-room HTTP snapshots on Sites D1. Clients predict movement, while server simulation owns collisions, damage, pickups and scores. This is arcade collision with simplified hulls, not per-brick physical fracture. Room capacity is eight, and online responsiveness under eight physical clients has not yet been measured. A menu does not pause a multiplayer match.

Site audience remains private to its owner. Inviting friends requires granting viewer access through Share; choosing a common room code alone does not grant access. No public access change was made.


### Current live results

The arena deployment succeeded with both generated D1 migrations packaged. The compiled Worker returned all 36 public assets byte-for-byte equal to source; captured designs, test reports and credentials are excluded from its asset table.

On September 12 the deployed OpenAI endpoint authenticated successfully and generated a fresh **Emerald Firewing Dragon**: 55 parts, seven joints, version 3, fly movement, flame weapon, medium armor, heavy mass, emitter `[0, 3.25, 4.85]`; GPT-5.4 model time **19.033 seconds**. The response is preserved in `validation/live/arena-dragon.json`. Its CPU three-view diagnostic shows a green body, head/snout/horns, golden wings, four legs and a long tail; this is a simple generated brick construction, with some loose appendage joins, not a finished authored model. No preset was substituted.

The actual response passed finite geometry, moving assembly, automatic mounting, flight, flame use, collection save/rebuild and original objective replay checks. An initial route through scenery destroyed the mount; a clear route then verified the generation/use loop. That distinction is preserved instead of calling the crash a generation failure.

The production multiplayer acceptance attempt stopped at **HTTP 401: “Sign in with ChatGPT to join the arena.”** The authorized automation bypass reaches private Site content but provides no `oai-authenticated-user-id`. No test room was joined. A bypass was not invented and client identity headers were not forged. The UI now offers the platform-owned top-level sign-in route when required. This test boundary prevents claiming live multi-client play or deployed D1 gameplay acceptance; the generated migrations and identical SQL were tested locally with three authenticated test identities and concurrent updates. Signed-in browser sessions across actual devices still need an owner/invited-viewer playtest. `validation/live/arena-acceptance.json` records the exact observed results without credentials.


## Movement, rendering and keyboard repair — September 12, 2026

Recovered the clean published source at `139e605109ed34e630258683f7a001bf89aa00a3`; a fresh authenticated fetch showed no newer source (0 commits ahead / 0 behind). The owner-only audience, Sites identity, OpenAI runtime secret and database migrations are preserved.

Observed causes and fixes:

- The foot capsule center was 0.05 m above its true half-height while the floor clamp discarded the corresponding movement. Small overlaps triggered radial recovery moves of about a meter. The collider center now matches its dimensions with Rapier's 0.03 m skin preserved. Walking no longer auto-steps or invokes radial teleport recovery.
- Arena overlap and sweep tolerances differed; rotating a walking box could repeatedly alternate between valid and overlapping poses. Walking uses a yaw-independent hull and one tolerance, with direct camera-relative movement. Mounted rotation still checks the hull against substantial scenery.
- Crates, targets, plaza steps and tree crowns no longer impede walkers; flight-ring markers and low decoration do not impede movement. Targets/crates remain hittable by attacks, and substantial cover still stops or damages mounts. Occupied cover waits before rebuilding; player contact nudges are capped; blocked mount assembly reports an error instead of relocating the player.
- Arena foot equipment mistakenly included the full bow constructor at the feet. The default kit now identifies as foot and renders only its two small blaster bricks, including legacy foot snapshots whose mode was bow. The center crosshair was removed.
- Desired and interpolated camera positions both receive a building/placement obstruction check. Snapshot presentation blends bounded position corrections, integrates slower frames properly and cannot render below ground on a delayed jump landing. Server collision/health authority is unchanged.
- Keyboard controls now use WASD/Shift on the left and J/K/L plus U/I/O on the right, with Q/E and arrows for camera/aim. Help, button badges, tooltips and this README agree. Browser modifiers, typing, dialogs, key-repeat and focus loss are guarded.

`npm run check` passed **81 checks**: original 16, generation 9, adventure 14, captured live replay 4, arena/SQLite API 22, DOM/scene 7, and movement/camera/presentation 9. The committed movement regression replays 3,600 frames on the actual island in each simulation with a maximum allowed 0.35 m step; observed maxima were 0.1696 m in Explore and 0.1575 m in Arena. It also verifies small-prop traversal with house blocking, blocked construction without relocation, gentle player contact, 20 FPS prediction, delayed snapshot blending, jump landing height, and a real house corner camera orbit.

An independent extended deterministic replay used seed 48, changed yaw every 30 frames, and ran 36,000 frames at 60 Hz in each simulation: zero recovery jumps and zero obstacle overlaps; largest steps 0.1703 m (Explore) and 0.1575 m (Arena). Additional 60-second Arena paths at 15/20/30 FPS had no overlaps. These are numerical simulations, not browser frame-rate measurements. DOM geometry bounds prove the default blaster has no equipment below the jumping actor's feet and an actual equipped bow still exists; canvas is stubbed for labels.

Requested browser testing was attempted again through the supported managed preview. The cloud browser reached Brickwild's graphics fallback screen but WebGL creation failed: GL_VENDOR and GL_RENDERER were both `Disabled`, with `BindToCurrentSequence failed`. The failure screen and console were observed; browser settings were not bypassed. Actual rendered gameplay, microphone recognition, real multitouch and physical iPad performance remain unverified. No new paid generation call was needed for these changes; the four captured live outputs replay successfully, and the prior successful live dragon result remains documented above. No claim of live multi-device acceptance is made.

The repaired self-contained Worker contains 39 public assets. The compiled handler returned all 39 byte-for-byte equal to source; validation files, environment files and the hosting manifest returned 404. A public source scan found no API key literals or server-secret references. Both existing generated D1 migrations are present in the packaged output.


## Unobstructed movement and arrow controls — September 12, 2026

Continued from published version 9 (`d99de53d0bc2c5cbe9a57abd7896385200aec45f`); a fresh source fetch matched HEAD exactly. The latest request explicitly supersedes prior realistic/environmental collision requirements and the version-9 keyboard layout. Current private access, server secrets, original procedural assets and D1 migrations are retained.

Movement now integrates positions directly. Rapier in Explore provides static weapon-cover/placement queries only. Both Explore and Arena remove scenery sweeps, turn rejection, fit checks for mounts, impact slowdown, environmental crash ejection, player separation/ram damage and hard-landing crashes. Trees, houses, unnamed scenery boxes and generated placements never alter player or mount position, heading, velocity or health. Ground/altitude and island boundaries remain. World geometry also no longer clips the camera. Weapon damage, mount shields, defeat/respawn and shot occlusion still operate. Static placements still find spacing among scenery. A character/camera may pass inside a visible building and be obscured while there.

The creation dock contains Imagine and Speak only. Preset buttons and 1–4 keyboard builds are removed; decorative compass/caption and auto-run controls are hidden, as is the redundant Arena entry button while joined. Original models and activities remain in source. Main actions are **↑ jump/rise, ↓ lower, ← Speak toggle, → attack/use**. Enter opens Imagine with the text area focused; Backspace dismounts. Q/E looks, R/F adjusts vertical aim, H toggles the Hotkeys panel, and P/Escape opens/closes menus. The visible panel uses an arrow-key layout and a complete control list, and remains available on small-screen CSS layouts.

The full suite passed **83 checks**: original 16, generation 9, adventure 14, captured live replay 4, Arena/SQLite API 22, DOM/scene 8, movement/camera/presentation 10. The changed checks now require pass-through movement and no contact effects while retaining separate tree/house projectile cover checks. The old collision/ram assertions were deliberately replaced to match the new request, not simply omitted.

The committed movement check compares actual full-world scenery (plus unnamed obstacles and generated placement) with an empty world for foot, car, plane and a captured generated dragon, in both simulations. Positions, heading, velocity and health match exactly throughout each path. An independent read-only check ran 28 comparisons, 210 frames each, across house/tree geometry and foot, bow, sword, car, plane, generated driving and flying modes, including placed/unnamed obstacles: all matched exactly. Mounts also assemble inside cover without teleporting. Seeded 3,600-frame walking paths had maximum steps of 0.1633 m in Explore and 0.1575 m in Arena, with no recovery jumps. Additional tests retain island/ground limits, jumping, low-frame-rate prediction, correction smoothing, movement during assembly, and all original objective rewards.

Browser checks used the supported managed preview. The full game still displayed the graphics-unavailable fallback because the cloud browser cannot initialize WebGL. To verify the UI independently, `validation/ui-harness.html` is served only by Vite at `/__controls`. It imports the actual production UI, input and CSS, supplies a local display-only Arena fixture, and starts no renderer, network match, microphone or OpenAI call. In the browser, the compact dock rendered without presets; the Hotkeys button displayed the visual modal; the close button and Escape dismissed it; H reopened it; Enter opened Imagine with the text field focused; ← and ↑ invoked the intended callbacks. Screenshot inspection confirmed the desktop Hotkeys panel is readable and scrollable. These are real browser UI checks, not rendered 3D or live multiplayer acceptance. Physical iPad, real multitouch, microphone recognition and gameplay rendering remain unverified.

No new OpenAI generation was purchased for this revision; all four captured live designs passed replay/assembly/use checks. No authentication, access-policy or billing guard was changed.

The self-contained Worker build contains 38 public assets. All 38 were fetched through the compiled handler and matched source byte-for-byte. The controls harness, validation fixtures, environment files, hosting manifest and removed collision-policy module return 404 in production packaging. Public source contains no API key literal or server-secret reference; existing migrations remain included.


## Latest fix: no physics, bounce or automatic position recovery

Baseline before this revision: published source `425cd19bc40aec1234e1b78cdb0dbd78b69418fb`, recovered clean with the remote branch at the same revision. The preceding collision removal was insufficient. A direct reproduction showed an idle plane coasting **17.456 metres in two seconds** after movement release. Code inspection also found hard display snaps on kit changes/large corrections, snapshot replacement without input acknowledgement, and 401/410 handling that silently returned the player to the Explore plaza.

The new `movement.js` supplies direct controls in Explore, authoritative Arena movement and local prediction. Rapier initialization, bodies, query world and public engine asset are removed; the engine is archived outside public output for recovery and its license remains. Shot cover and clear placement use only segment/box geometry math. There is no gravity, acceleration, coasting, automatic takeoff, ground bounce, knockback, or auto-run. Jump is a single 0.64-second, 1.25-metre scripted lift/return. Flight height changes only on ↑/↓; an airborne dismount holds position and exposes ↓/touch Lower. The camera follows directly. Body bob, assembly arcs, idle joint sway, plane rocking, marker bob, decorative shot arcs, and physical debris are gone; hit/reward flashes shrink in place.

`movement-stream.js` bounds compact numbered frames. Updated clients negotiate protocol 1 at join, buffer at most 90 frames, and submit input only. The server advances each accepted frame once with wall-clock movement credit; gaps, invalid values and wrong respawn epochs cannot advance it. The client replays unacknowledged input instead of extrapolating RTT over replaced coordinates. Kit changes and ordinary late snapshots do not trigger teleports. Residual correction is presentation-only and converges during deliberate movement; idle display positions are held. Expired connections keep the last pose and vitals with a Rejoin action. A late join after Leave cannot reactivate. Explicit Leave preserves coordinates; death/respawn remains an intentional game rule with a new spawn epoch.

The latest complete `npm run check` passes **98 checks**: original 16, generation 9, adventure 14, captured live replay 4, Arena/SQLite API 23, DOM/scene 9, movement/camera 10, direct movement/network 13. Expected old automatic-takeoff/fall assertions now require explicit rise/lower. All 15 objectives still award exactly 226 bricks, original models work, and all four captured live designs replay successfully. The new HTTP test executes actual SQLite-backed routes with negotiated movement frames and checks malformed/oversized rejection.

The added network suite runs the actual client and authoritative movement against a deterministic transport with **50, 250 and 600 ms simulated round-trip latency**. Turning/walking tests have a largest displayed step of 0.1167 m at 60 Hz and **zero drift after release**; server position catches up exactly. Further cases cover plane assembly while moving, manual ascent, hovering and airborne dismount; a lost response retried without duplicate motion; one jump without rebound; no idle snap on an 18-metre ordinary correction or kit change; HTTP 401/410 retaining pose; late join cancellation; input sequence/rate bounds; and rejection of pre-death frames after a real respawn. All original and generated modes stay fixed for 300 idle frames even when given stale speed, vertical velocity and auto-run state.

These are automated Node, DOM and Three.js scene-math checks with simulated latency, not a rendered game or physical-device playtest. The cloud browser previously failed WebGL initialization; full 3D rendering, physical iPad/multitouch, and production multiplayer responsiveness remain unverified. The earlier successful browser HUD checks are historical and were not rerun for this fix. The new DOM check confirms retained vitals, the visible Rejoin action and touch Lower after airborne dismount. No paid OpenAI request was made for this movement revision. Existing server secret, model configuration, authentication, Site audience, session/rate limits and D1 schema are preserved; movement packets alone have a bounded 16 KB limit to carry input frames.

The final self-contained build includes **40 public assets**, all fetched through the compiled Worker and matched byte-for-byte to source. Rapier/archive paths, the controls harness, validation fixtures, environment files and hosting manifest return 404. Unauthenticated Arena access remains HTTP 401. No public JavaScript contains an API-key literal or server-secret reference; both existing migrations are packaged. The historical expired-seat API fixture was made deterministic by advancing its simulated clock after expiry, avoiding a same-millisecond false negative.
# September 13 arena implementation — current evidence

Recovery: existing local/Sites source `41c119821227b8f74280a425f7c1716faaba3f39`, Site version 11, clean checkout. GitHub `main` at `034917f1b4c376037cc896ab80abf5d3c91e7973`; all 82 blob hashes matched. All 98 baseline checks passed before editing.

Entry/capacity stage: 23 Arena/SQLite checks and nine DOM/scene checks pass. Updated assertions require 40 simulation players and 20 API fixture sessions in one room, with no ninth-player rejection. The production identity boundary is unchanged; API tests use isolated test principals in memory, never forged headers against a live Site.

`validation/capacity-entry.json` records 10/20/40 concurrent synthetic writers, five bursts per size, with zero failures. With a simulated 5 ms delay on each storage operation, p95 mutation latencies were 106/213/420 ms; snapshots were about 9.7/19.4/38.8 KB. This demonstrates snapshot serialization as a bottleneck, not a measured production player ceiling. Real D1, cross-isolate contention, network transit and rendering are not included. No production concurrency claim is made.
