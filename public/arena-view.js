import * as THREE from './vendor/three.module.js';
import {character,car,plane,bow,sword,box,brick,material,C} from './models.js';
import {createGeneratedModel} from './generated-model.js';
const colors=['#ff784f','#65d8cc','#aa87ff','#f4cd52','#76b9ff','#ff89bc','#b4df6a','#f59f68'];
const shotGeo=new THREE.SphereGeometry(.14,7,5),fireGeo=new THREE.SphereGeometry(.28,8,6),auraGeo=new THREE.SphereGeometry(1,14,10);
function disposeOriginal(g){g.traverse(o=>{if(o.geometry&&!o.geometry.userData.shared)o.geometry.dispose();});g.removeFromParent();}
export function createArenaView(scene,camera){
 const root=new THREE.Group();root.visible=false;scene.add(root);const actors=new Map(),shots=new Map(),drops=new Map(),placed=new Map(),effects=[];let eventCursor=0;
 function kitModel(kit,cache){if(kit.blueprintId){const blueprint=cache.get(kit.blueprintId);if(!blueprint)return null;try{return createGeneratedModel(blueprint);}catch{return null;}}const model=kit.id==='foot'?{group:new THREE.Group()}:({car,plane,bow,sword}[kit.mode]||(()=>({group:new THREE.Group()})))();if(kit.id==='foot'){brick(model.group,.75,1.1,.55,.22,.24,.8,C.teal,false);brick(model.group,.75,1.12,.98,.3,.3,.16,C.yellow,false);}const parts=model.group.children.map((mesh,i)=>({mesh,end:mesh.position.clone(),rotation:mesh.quaternion.clone(),scale:mesh.scale.clone(),delay:i/model.group.children.length*.55,index:i}));return {...model,assemble(progress){for(const p of parts){const t=THREE.MathUtils.clamp((progress-p.delay)/(1-p.delay),0,1),ease=1-Math.pow(1-t,3),angle=p.index*2.399;p.mesh.position.set(Math.sin(angle)*(3+p.index%3),2.5+p.index%5*.4,Math.cos(angle)*(3+p.index%3)).lerp(p.end,ease);p.mesh.quaternion.setFromAxisAngle(new THREE.Vector3(0,1,0),(1-ease)*Math.PI*2).multiply(p.rotation);p.mesh.scale.copy(p.scale).multiplyScalar(.4+.6*ease);}},dispose(){disposeOriginal(model.group);}};}
 function createActor(p,i){const g=new THREE.Group(),pilot=character(),canvas=document.createElement('canvas');canvas.width=256;canvas.height=100;const texture=new THREE.CanvasTexture(canvas),label=new THREE.Sprite(new THREE.SpriteMaterial({map:texture,depthTest:false}));label.scale.set(3.7,1.45,1);label.renderOrder=5;const aura=new THREE.Mesh(auraGeo,new THREE.MeshBasicMaterial({color:colors[i%colors.length],transparent:true,opacity:.16,wireframe:true,depthWrite:false}));g.add(pilot.group,aura,label);root.add(g);const a={g,pilot,label,canvas,texture,aura,model:null,modelId:null,pending:null,pendingId:null,lastLabel:'',position:new THREE.Vector3(p.x,p.y,p.z)};actors.set(p.id,a);return a;}
 function renderKit(a,p,cache,time){if(a.modelId!==p.kit.id||(!a.model&&p.kit.blueprintId&&cache.has(p.kit.blueprintId))){a.model?.dispose();a.model=kitModel(p.kit,cache);a.modelId=p.kit.id;if(a.model)a.g.add(a.model.group);}
  const model=a.model;if(model){model.update?.(time/1000,1,p.speed/12);model.wheels?.forEach(w=>w.rotation.x=time/1000*p.speed);if(model.prop)model.prop.rotation.z=time/65;const carry=!p.kit.stats.mounted;if(carry&&p.kit.blueprintId){model.group.rotation.z=-Math.max(0,1-(time-p.actionAt)/400)*.8;model.group.position.set(.76,1.1,.3).sub(model.grip.clone().applyQuaternion(model.group.quaternion));}else if(p.kit.mode==='bow'&&p.kit.id!=='foot')model.group.position.set(-.85,1.26,.64);else if(p.kit.mode==='sword')model.group.position.set(.81,1.06,.37);}
  a.pilot.group.scale.setScalar(p.kit.stats.mounted?.7:1);if(p.kit.stats.mounted){const seat=model?.seat||new THREE.Vector3(0,p.kit.mode==='plane'?1.55:1.33,-.5);a.pilot.group.position.copy(seat);a.pilot.group.position.y-=.48;a.pilot.legs.forEach(l=>l.rotation.x=-1.45);}else{a.pilot.group.position.set(0,0,0);a.pilot.legs.forEach((l,i)=>l.rotation.x=Math.sin(time/85+i*Math.PI)*Math.min(.65,p.speed/12));}a.pilot.arms.forEach((arm,i)=>arm.rotation.x=p.building?-1.3+Math.sin(time/50+i)*.4:Math.sin(time/85+i*Math.PI)*Math.min(.4,p.speed/14));
  if(p.building){const id=p.building.kit.id;if(a.pendingId!==id||(!a.pending&&cache.has(id))){a.pending?.dispose();a.pending=kitModel(p.building.kit,cache);a.pendingId=id;if(a.pending)a.g.add(a.pending.group);}if(a.pending){const progress=THREE.MathUtils.clamp((time-p.building.starts)/(p.building.ends-p.building.starts),0,1);if(a.pending.update)a.pending.update(time/1000,progress,p.speed/12);else a.pending.assemble(progress);}}else if(a.pending){a.pending.dispose();a.pending=null;a.pendingId=null;}
 }
 function update(snapshot,local,cache,dt,time){root.visible=!!snapshot;if(!snapshot)return;const live=new Set();snapshot.players.forEach((remote,i)=>{const p=remote.id===snapshot.self&&local?local:remote;live.add(p.id);const a=actors.get(p.id)||createActor(p,i);a.g.visible=p.health>0;if(!a.g.visible)return;a.position.lerp(new THREE.Vector3(p.x,p.y,p.z),p.id===snapshot.self?1:1-Math.exp(-dt*12));a.g.position.copy(a.position);a.g.rotation.y+=Math.atan2(Math.sin(p.yaw-a.g.rotation.y),Math.cos(p.yaw-a.g.rotation.y))*(1-Math.exp(-dt*14));renderKit(a,p,cache,time);
   const height=Math.max(3,p.kit.stats.collision[1]+1.8);a.label.position.set(0,height,0);a.label.visible=p.id!==snapshot.self;const label=[p.name,Math.ceil(p.health),Math.ceil(p.mountHealth),p.kills].join(':');if(label!==a.lastLabel){a.lastLabel=label;const ctx=a.canvas.getContext('2d');ctx.clearRect(0,0,256,100);ctx.fillStyle='#16392e';ctx.fillRect(0,0,256,100);ctx.font='bold 25px sans-serif';ctx.textAlign='center';ctx.fillStyle='#fffaf0';ctx.fillText(p.name,128,30,238);ctx.fillStyle='#40594f';ctx.fillRect(12,44,232,17);ctx.fillStyle='#81de86';ctx.fillRect(12,44,232*p.health/100,17);if(p.mountHealth>0){ctx.fillStyle='#73caff';ctx.fillRect(12,70,232*p.mountHealth/Math.max(1,p.kit.stats.mountMax),10);}a.texture.needsUpdate=true;}a.aura.visible=time<p.defenseUntil||time<p.protectedUntil;a.aura.position.y=p.kit.stats.collision[1]/2;a.aura.scale.set(Math.max(1,p.kit.stats.collision[0]/2+.3),p.kit.stats.collision[1]/2+.3,Math.max(1,p.kit.stats.collision[2]/2+.3));a.aura.rotation.y=time/1700;
  });for(const [id,a]of actors)if(!live.has(id)){a.model?.dispose();a.pending?.dispose();disposeOriginal(a.pilot.group);a.texture.dispose();a.label.material.dispose();a.aura.material.dispose();a.g.removeFromParent();actors.delete(id);}
  const activeShots=new Set();for(const b of snapshot.projectiles){activeShots.add(b.id);let m=shots.get(b.id);if(!m){m=new THREE.Mesh(b.weapon==='flame'?fireGeo:shotGeo,new THREE.MeshBasicMaterial({color:b.color}));shots.set(b.id,m);root.add(m);}const lead=Math.max(0,Math.min(.25,(time-snapshot.time)/1000));m.position.set(b.x+b.vx*lead,b.y+b.vy*lead,b.z+b.vz*lead);if(b.weapon==='flame'){const scale=1+lead*6;m.scale.set(scale,scale,scale*1.7);}else if(b.weapon==='bow'){m.scale.set(.35,.35,4);m.lookAt(m.position.x+b.vx,m.position.y+b.vy,m.position.z+b.vz);}}
  for(const [id,m]of shots)if(!activeShots.has(id)){m.removeFromParent();m.material.dispose();shots.delete(id);}
  const liveDrops=new Set();for(const d of snapshot.drops){liveDrops.add(d.id);let g=drops.get(d.id);if(!g){g=new THREE.Group();const color=d.type==='health'?0xec6556:d.type==='defense'?0x5fc9eb:0xf6cd45;brick(g,0,0,0,1.25,1.1,1.25,color);box(g,0,.57,0,.26,.05,1.1,C.cream);box(g,0,.57,0,1.1,.05,.26,C.cream);const canopy=new THREE.Group();for(let i=-2;i<=2;i++)brick(canopy,i*.48,2.6-Math.abs(i)*.17,0,.5,.15,1.9,i%2?C.cream:color,false);for(const x of [-.55,.55])box(canopy,x,1.4,0,.04,2,.04,C.cream);g.add(canopy);g.userData.canopy=canopy;root.add(g);drops.set(d.id,g);}const remaining=Math.max(0,(d.lands-time)/(d.lands-d.born));g.position.set(d.x,.8+remaining*22,d.z);g.rotation.y=time/1700;g.userData.canopy.visible=remaining>0;}
  for(const [id,g]of drops)if(!liveDrops.has(id)){disposeOriginal(g);drops.delete(id);}
  const livePlaced=new Set();for(const p of snapshot.placed||[]){livePlaced.add(p.id);let model=placed.get(p.id);if(!model&&cache.has(p.blueprintId)){try{model=createGeneratedModel(cache.get(p.blueprintId));}catch{continue;}model.group.position.set(p.x,0,p.z);root.add(model.group);placed.set(p.id,model);}if(model){model.group.visible=!snapshot.destroyed[p.id];model.update(time/1000);}}for(const [id,m]of placed)if(!livePlaced.has(id)){m.dispose();placed.delete(id);}
  for(let i=effects.length-1;i>=0;i--){const e=effects[i];e.life-=dt;e.mesh.scale.setScalar(Math.max(0,e.life/.4));if(e.life<=0){e.mesh.geometry.dispose();e.mesh.removeFromParent();effects.splice(i,1);}}
 }
 function effect(e){if(!['break','crash','ko','hit','pickup','impact','swing'].includes(e.type))return;const amount=['break','crash','ko'].includes(e.type)?28:8;for(let i=0;i<amount&&effects.length<160;i++){const mesh=new THREE.Mesh(new THREE.BoxGeometry(.2,.18,.25),material(e.color||C.yellow));const angle=i*2.399;mesh.position.set((e.x||0)+Math.sin(angle)*.8,(e.y||1)+(i%4)*.2,(e.z||0)+Math.cos(angle)*.8);root.add(mesh);effects.push({mesh,life:.4});}}
 function clear(){update({players:[],projectiles:[],drops:[],placed:[],destroyed:{}},null,new Map(),0,0);root.visible=false;for(const e of effects){e.mesh.geometry.dispose();e.mesh.removeFromParent();}effects.length=0;}
 return {update,effect,clear};
}
